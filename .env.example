# Required
TELEGRAM_BOT_TOKEN=123456789:replace-me
GEMINI_API_KEY=replace-me

# Docker Compose defaults to its local postgres service.
DATABASE_URL=postgresql://pricewatch:pricewatch@postgres:5432/pricewatch
POSTGRES_DB=pricewatch
POSTGRES_USER=pricewatch
POSTGRES_PASSWORD=change-this-password

# SearchPlan model: only used when a product is added/refreshed, never every 4 minutes.
GEMINI_MODEL=gemini-3.5-flash-lite

# Worker
SCAN_INTERVAL_SECONDS=240
WORKER_BATCH_SIZE=20
LEASE_SECONDS=180
MARKETPLACE_TIMEOUT_SECONDS=20
# WORKER_ID can be omitted; container hostname is used.
# WORKER_ID=pricewatch-worker-1

# Telegram/outbox
OUTBOX_BATCH_SIZE=50
TELEGRAM_POLL_TIMEOUT=30
